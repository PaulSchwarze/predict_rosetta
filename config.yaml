# Data & Paths
csv_path: "C:/Users/paulb/PycharmProjects/METL_GLOBAL/data/data_split_normalized_combined.csv"
save_dir: "models"

# Model Settings
encoder_name: "esm2"     # "esm2" or "prot_t5"
hidden_dims: [512, 256, 128, 64]
mlp_dropout: 0.3

# LoRA Settings
lora_r: 8
lora_alpha: 16
lora_dropout: 0.05

# Training Hyperparameters
max_samples: null        # Use null instead of None in YAML
batch_size: 16
epochs: 20
lr: 0.0001
weight_decay: 0.0001
patience: 5

# Reproducibility
seed: 42